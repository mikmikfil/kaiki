import { defineConfig } from 'vite';
import laravel from 'laravel-vite-plugin';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
    plugins: [
        laravel({
            input: [
                'resources/css/app.css',
                'resources/js/app.js',
                // The boarding page's camera and QR decoder. Its own entry, so
                // it is one self-contained file the boarding service worker can
                // precache (see BoardingController::scannerScriptUrl()).
                'resources/js/boarding-scanner.js',
            ],
            refresh: true,
        }),
        tailwindcss(),
    ],
    server: {
        watch: {
            ignored: ['**/storage/framework/views/**'],
        },
    },
});
